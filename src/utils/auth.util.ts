import * as JWT from 'jsonwebtoken'

interface ITokenPair{
          accessToken: string;
          refreshToken: string;
}

export const createTokenPair = (payload: any, publicKey, privateKey) => {
         try {
                  // const accessToken = 
         } catch (error) {
                  
         }
};
